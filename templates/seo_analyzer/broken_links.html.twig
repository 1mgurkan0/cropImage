{% block stylesheets %}
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #5d67e6;
            --background-color: #f0f4f8;
            --card-bg-color: #ffffff;
            --text-color: #34495e;
            --secondary-text-color: #7f8c8d;
            --border-color: #e0e6ed;
            --shadow-light: 0 4px 15px rgba(0, 0, 0, 0.05);
            --shadow-hover: 0 8px 25px rgba(0, 0, 0, 0.1);
        }
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
        }
        .converter-card {
            background-color: var(--card-bg-color);
            border-radius: 24px;
            box-shadow: var(--shadow-light);
            padding: 2.5rem;
            border: none;
        }
        .card-title {
            font-weight: 700;
            color: var(--text-color);
            margin-bottom: 0.5rem;
        }
        .submit-btn {
            background-color: var(--primary-color);
            border: none;
            border-radius: 12px;
            padding: 12px 24px;
            font-weight: 600;
            transition: background-color 0.3s ease, transform 0.2s ease;
            color: #ffffff;
        }
        .submit-btn:hover {
            background-color: #3871c2;
            transform: translateY(-2px);
        }
        h1, h2, h3 {
            color: var(--text-color);
        }
        label {
            font-weight: 600;
            color: var(--text-color);
            margin-bottom: 0.5rem;
        }
        input[type="url"] {
            border-radius: 12px;
            border: 1px solid var(--border-color);
            padding: 10px 15px;
            transition: border-color 0.3s ease;
            width: 100%;
        }
        input[type="url"]:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(74, 144, 226, 0.25);
        }
        .hidden {
            display: none;
        }
        .table {
            background-color: var(--card-bg-color);
            border-radius: 12px;
            overflow: hidden;
        }
        .table thead th {
            background-color: var(--primary-color);
            color: #ffffff;
            border-color: var(--primary-color);
        }
        .table tbody tr:nth-child(even) {
            background-color: var(--background-color);
        }
        .table tbody tr td {
            color: var(--text-color);
            border-color: var(--border-color);
        }
        .status-error {
            color: #e74c3c;
            font-weight: 600;
        }
        a {
            color: var(--primary-color);
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        .feature-card {
            background-color: var(--card-bg-color);
            border-radius: 24px;
            box-shadow: var(--shadow-light);
            transition: transform 0.3s, box-shadow 0.3s;
            text-decoration: none;
            border: none;
        }
        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-hover);
        }
        .card-icon svg {
            width: 64px;
            height: 64px;
            color: var(--primary-color);
        }
        .card-title-link {
            font-weight: 600;
            color: var(--text-color);
            text-decoration: none;
        }
        .card-text {
            color: var(--secondary-text-color);
        }
        .main-container {
            padding-top: 1rem;
            padding-bottom: 1rem;
        }
    </style>
{% endblock %}

<main class="container my-5">
    <div class="card p-4 shadow-lg converter-card">
        <hgroup>
            <h1 class="card-title text-center mb-4">Broken Link Checker</h1>
            <h2 class="intro-text lead mb-5 text-center">Check all links on a page and detect errors like 404s.</h2>
        </hgroup>

        <form id="broken-link-form">
            <div class="mb-3">
                <label for="url_to_check" class="form-label">URL to Check</label>
                <input type="url" id="url_to_check" name="url_to_check" placeholder="https://example.com/a-page" required>
            </div>
            <button type="submit" id="submit-btn" class="submit-btn w-100">Find Broken Links</button>
        </form>

        <div id="progress-container" class="hidden mt-4">
            <progress id="progress-bar" class="w-100"></progress>
            <p id="progress-text" class="text-center"></p>
        </div>

        <div id="results-container" class="hidden mt-4">
            <h3 class="card-title mb-3">Results</h3>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                    <tr>
                        <th>Found Link</th>
                        <th>HTTP Status Code</th>
                        <th>Status</th>
                    </tr>
                    </thead>
                    <tbody id="results-tbody"></tbody>
                </table>
            </div>
        </div>
    </div>
</main>

<div class="container main-container text-center py-4">
    <div class="row g-4">
        <div class="col-md-4">
            <a href="{{ path('app_image_compress') }}" class="text-decoration-none">
                <div class="card p-5 feature-card h-100 d-flex flex-column align-items-center justify-content-center">
                    <span class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-arrows-collapse" viewBox="0 0 16 16">
                            <path fill-rule="evenodd" d="M1 8a.5.5 0 0 1 .5-.5h13a.5.5 0 0 1 0 1h-13A.5.5 0 0 1 1 8m13-3.5a.5.5 0 0 1-.5.5h-13a.5.5 0 0 1 0-1h13a.5.5 0 0 1 .5.5m-13 7a.5.5 0 0 1 .5-.5h13a.5.5 0 0 1 0 1h-13a.5.5 0 0 1-.5-.5"/>
                        </svg>
                    </span>
                    <h2 class="card-title-link mt-3">Image Compression</h2>
                    <p class="card-text text-center mt-2">Gain speed by reducing file size. Compress while maintaining quality.</p>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('app_image_convert') }}" class="text-decoration-none">
                <div class="card p-5 feature-card h-100 d-flex flex-column align-items-center justify-content-center">
                    <span class="card-icon">
                       <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-files" viewBox="0 0 16 16">
                            <path d="M13 10H3c-.55 0-1-.45-1-1V3c0-.55.45-1 1-1h10c.55 0 1 .45 1 1v6c0 .55-.45 1-1 1zM3 3v6h10V3H3zm12-2H4c-.55 0-1 .45-1 1v1h11c.55 0 1 .45 1 1v11h1c.55 0 1-.45 1-1V2c0-.55-.45-1-1-1z"/>
                        </svg>
                    </span>
                    <h2 class="card-title-link mt-3">Image Converter</h2>
                    <p class="card-text text-center mt-2">Fast and easy conversion to different formats.</p>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('app_image_crop') }}" class="text-decoration-none">
                <div class="card p-5 feature-card h-100 d-flex flex-column align-items-center justify-content-center">
                    <span class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-arrow-repeat" viewBox="0 0 16 16">
                            <path d="M11.534 7h3.932a.25.25 0 0 1 .192.41l-1.92 2.9A.25.25 0 0 1 13.52 10.5h-3.932a.25.25 0 0 1-.192-.41l1.92-2.9a.25.25 0 0 1 .192-.09m-7.068 0h3.932a.25.25 0 0 0 .192-.41L6.48 4.6A.25.25 0 0 0 6.48 4.6L4.56 1.7A.25.25 0 0 0 4.368 1.5h-3.932a.25.25 0 0 0-.192.41l1.92 2.9a.25.25 0 0 0-.192.41L1.76 10.5a.25.25 0 0 0 .192.41h3.932a.25.25 0 0 0 .192-.41l-1.92-2.9a.25.25 0 0 0-.192-.09m7.068 0h3.932a.25.25 0 0 0 .192-.41L13.52 4.6A.25.25 0 0 0 13.52 4.6L11.6 1.7A.25.25 0 0 0 11.408 1.5h-3.932a.25.25 0 0 0-.192.41l1.92 2.9a.25.25 0 0 0-.192.41L7.76 10.5a.25.25 0 0 0 .192.41h3.932a.25.25 0 0 0 .192-.41l-1.92-2.9a.25.25 0 0 0-.192-.09m-7.068 0h3.932a.25.25 0 0 0 .192-.41L6.48 4.6A.25.25 0 0 0 6.48 4.6L4.56 1.7A.25.25 0 0 0 4.368 1.5h-3.932a.25.25 0 0 0-.192.41l1.92 2.9a.25.25 0 0 0-.192.41L1.76 10.5a.25.25 0 0 0 .192.41h3.932a.25.25 0 0 0 .192-.41l-1.92-2.9a.25.25 0 0 0-.192-.09"/>
                        </svg>
                    </span>
                    <h2 class="card-title-link mt-3">Image Cropper</h2>
                    <p class="card-text text-center mt-2">Highlight the area you want. Crop out unnecessary details and simplify.</p>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('seo_analyzer') }}" class="text-decoration-none">
                <div class="card p-5 feature-card h-100 d-flex flex-column align-items-center justify-content-center">
                    <span class="card-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                            <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0"/>
                        </svg>
                    </span>
                    <h2 class="card-title-link mt-3">SEO Analyzer</h2>
                    <p class="card-text text-center mt-2">Instantly analyze your website's SEO performance. See your shortcomings and discover opportunities for improvement!</p>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('app_character_counter') }}" class="text-decoration-none">
                <div class="card p-5 feature-card h-100 d-flex flex-column align-items-center justify-content-center">
            <span class="card-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-journal-text" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M5 8h6v1H5V8zm0-2h6v1H5V6z"/>
                    <path d="M3 2.5v11a.5.5 0 0 0 .5.5H13v1H3.5A1.5 1.5 0 0 1 2 13.5v-11A1.5 1.5 0 0 1 3.5 1H13v1H3.5a.5.5 0 0 0-.5.5z"/>
                    <path d="M14 1.5a.5.5 0 0 1 .5.5v13a.5.5 0 0 1-1 0v-13a.5.5 0 0 1 .5-.5z"/>
                </svg>
            </span>
                    <h2 class="card-title-link mt-3 text-center">Text Counter</h2>
                    <p class="card-text text-center mt-2">Easily find out the number of characters and words in the text you enter.</p>
                </div>
            </a>
        </div>
    </div>
</div>


{% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        $(function() {
            const form = $('#broken-link-form');
            const submitBtn = $('#submit-btn');
            const progressContainer = $('#progress-container');
            const progressBar = $('#progress-bar');
            const progressText = $('#progress-text');
            const resultsContainer = $('#results-container');
            const resultsTbody = $('#results-tbody');

            form.on('submit', function(e) {
                e.preventDefault();
                const url = $('#url_to_check').val();

                if (!url) {
                    alert('Please enter a valid URL.');
                    return;
                }

                submitBtn.attr('aria-busy', 'true').prop('disabled', true);
                resultsContainer.addClass('hidden');
                resultsTbody.empty();
                progressContainer.removeClass('hidden');
                progressText.text('Links are being scanned and checked, please wait...');

                $.post("{{ path('api_find_broken_links') }}", { url: url })
                    .done(function(response) {
                        if (response.success) {
                            displayResults(response.data);
                        } else {
                            alert('Error: ' + response.message);
                        }
                    })
                    .fail(function() {
                        alert('An error occurred while connecting to the server.');
                    })
                    .always(function() {
                        submitBtn.removeAttr('aria-busy').prop('disabled', false);
                        progressContainer.addClass('hidden');
                    });
            });

            function displayResults(links) {
                resultsContainer.removeClass('hidden');
                if (links.length === 0) {
                    resultsTbody.append('<tr><td colspan="3">No broken links found on the page.</td></tr>');
                    return;
                }

                links.forEach(link => {
                    const row = `
                    <tr>
                        <td><a href="${link.url}" target="_blank">${link.url}</a></td>
                        <td>${link.http_code}</td>
                        <td><span class="status-error">Broken</span></td>
                    </tr>
                `;
                    resultsTbody.append(row);
                });
            }
        });
    </script>
{% endblock %}
